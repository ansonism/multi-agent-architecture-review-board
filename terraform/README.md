# Terraform

Add actual infrastructure modules only when a concrete deployment target is selected. Do not create nonfunctional placeholder cloud resources. Keep providers/backends environment-specific and never commit state or secrets.
